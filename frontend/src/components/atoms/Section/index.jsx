import styled from 'styled-components';

// .sect — section padding wrapper used by the presets and browse sections.
export const Section = styled.section`
  padding: 14px 22px 0;
`;

export default Section;
