import HomePage from './pages/HomePage';

// The app is a single page (HomePage). Theme + global styles are provided in
// main.jsx via styled-components' ThemeProvider + GlobalStyle.
export default function App() {
  return <HomePage />;
}
